# material/_inbox

这里是**未完成身份确认的原始资料隔离区**。

规则：
- 不进入 `material/catalog.yaml`；
- 不作为器件选型、封装定稿、电气参数的权威来源；
- 每个子目录必须说明“为什么还不能归档”；
- 一旦确认具体型号，资料迁入正式 `material/<category>/<id>/`，同步 item.yaml / INDEX.md 后删除该 inbox 子目录。

当前：
- `mx-hotswap-socket/`：疑似 CPG151101S11 / MX 热插拔轴座相关资料；已有 KiCad 私有 footprint/3D，但库存物料身份与数量尚未建立正式 item，因此暂不强行归档。
